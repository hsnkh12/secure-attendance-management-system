const express = require('express');
const router = express.Router();




router.get('/:offeredCourseID/:',)
router.post('/:offeredCourseID',)
router.put('/:offeredCourseID',)
router.delete('/:offeredCourseID',)

module.exports = {
    attendanceRoutes: router
}